# Research

Non-binding research workspace.

## Rules

- Research here does not become app truth until promoted into source code,
  release evidence, or durable docs.
- Generated `output*` folders and local artifacts should stay ignored unless a
  gate explicitly promotes them.
- Future consolidation should happen while app/research surfaces are being fixed,
  so useful work is reused instead of copied.
