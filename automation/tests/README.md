# Tests

End-to-end test workspace.

## Contents

| Path | Purpose |
|---|---|
| `e2e/` | Playwright tests. |

## Rules

- Unit tests currently live under `app/src/lib/__tests__` and run through Vitest.
- Playwright output belongs in ignored `test-results/` or `playwright-report/`.
- Playwright tests belong under root `automation/tests/`.
