# Next Chat Prompt: Gate 91 Formulaic Triangle Grid Geometry

Repo: `C:\Users\User\Documents\GitHub\limni-website`

Branch: `codex/gate88-mt5-lifecycle-protection-controls`

Continue from Codex/Poseidon session. Read recovery first:

1. `C:\Users\User\Documents\GitHub\freedom-ops\.codex\CODEX_SESSION.md`
2. `C:\Users\User\Documents\GitHub\freedom-ops\.codex\CODEX_CKB.md`
3. `AGENTS.md`
4. `docs/backlog/CURRENT_WORK.md`
5. `docs/research/gates/gate91/GATE91_FORMULAIC_TRIANGLE_GRID_GEOMETRY_HANDOFF_2026-07-03.md`

Open Gate 91 only:

`Gate 91: formulaic-triangle-grid-geometry`

Current objective:

Design and test a formulaic Triangle grid geometry algorithm. Do not optimize
clamp ladders. Build a durable equation that derives grid quantum, signal clock,
and reversion target from market structure.

Core direction from Freedom:

- Stop optimizing randomness.
- The final algorithm must use a formula that can stand the test of time.
- First pass should ignore direction to prove whether the grid geometry itself
  works without Candidate B, David, or any directional bias.
- David MA50 and ADR-event `0.075` are not final; they were research scaffold
  parameters.
- We are building something new: formulaic grid geometry first, direction later.

Preserve Gate 90D evidence:

- Best current strict no-Candidate-B risk rule:
  `pain_1q_stop_adds_before_profit_0_5q`.
- 2019 strict no-Candidate-B:
  baseline `+25.29` ADR units, `+1.17%` fixed-lot account, `-2.19%` DD,
  PF `1.230`;
  pain-stop `+32.98` ADR units, `+2.29%` fixed-lot account, `-0.67%` DD,
  PF `1.712`.
- 2026 strict no-Candidate-B:
  baseline `-5.58` ADR units, `-0.12%` fixed-lot account, `-2.39%` DD,
  PF `0.974`;
  pain-stop `+11.06` ADR units, `+0.55%` fixed-lot account, `-1.34%` DD,
  PF `1.196`.
- Hold-red and max-3D hold-red are rejected. Daily flatten stays.
- ADR units are movement quality. Fixed-lot account return is secondary cash
  sanity. Add raw market-percent movement columns before final replay freeze.

Gate 91 first task:

1. Inspect current Gate 90 runner and Triangle helper functions.
2. Add a new formulaic, directionless geometry scaffold behind a new activation
   id.
3. Define `Q = formula(session range, path efficiency, cost floor, current
   volatility)`.
4. Derive signal event brick from Q, not fixed `0.075`.
5. Avoid fixed David MA50 in the first pass.
6. Use daily flatten and the pain-first stop-add risk layer.
7. Run one-pair smoke, then bounded 2019 and 2026 A/B diagnostics only.

Frozen:

- No MT5/live/app work.
- No promotion.
- No red-news implementation.
- No 2020/year-by-year expansion.
- No full matrix restart.
- No full seven-pair handshake gate.
- No Candidate B direction dependency in the first Gate 91 pass.
